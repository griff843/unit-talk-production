{
  "name": "@unit-talk/config",
  "version": "1.0.0",
  "description": "Shared configuration management for Unit Talk Platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "echo 'No lint configured for config package'",
    "test": "jest",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "configuration",
    "environment",
    "shared"
  ],
  "author": "Unit Talk Team",
  "license": "PROPRIETARY",
  "dependencies": {
    "@unit-talk/contracts": "workspace:*",
    "@unit-talk/shared": "workspace:*",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  },
  "files": [
    "dist/**/*",
    "src/**/*"
  ]
}
