## 🧠 Copilot / AI Assistant Guidelines
When using Copilot to fix TypeScript errors:
- Only change types, never core logic
- Use `AgentExecutionPayload` and `BaseActivityParams`
- Add missing types and imports, but don’t modify functionality
